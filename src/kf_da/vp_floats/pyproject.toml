[project]
name = "vpfloat"
version = "0.1.0"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["vp_py_utils"]

[tool.setuptools.packages.find]
where = ["."]
include = ["vpfloat*"]
